# What it does

This is a 6 wheeled driverless car. Using Ultrasonic sensor as the main sensor. It detects any object and changes the path to avoid the obstacle. The motor driver used is L293D motor driver. This is done using VSD Squadron FPGA Mini.
